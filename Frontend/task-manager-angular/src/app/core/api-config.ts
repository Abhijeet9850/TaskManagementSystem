// Single source of truth for the backend's base URL.
// Update ONLY this file if your backend runs on a different port —
// check the console output when you run `dotnet run` to confirm the actual port.
export const API_BASE_URL = 'https://localhost:7284/api';
